<resources>
    <!-- Base application theme. -->
    <!-- NoActionBar, because we use MaterialToolbars inside Fragments (NavHost) -->
    <style name="Base.Theme.ProjectWorldWisdom" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.ProjectWorldWisdom" parent="Base.Theme.ProjectWorldWisdom" />
</resources>